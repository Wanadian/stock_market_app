import 'package:flutter/material.dart';

class StockPurchased extends StatefulWidget {
  const StockPurchased({Key? key}) : super(key: key);

  @override
  State<StockPurchased> createState() => _StockPurchasedState();
}

class _StockPurchasedState extends State<StockPurchased> {

  Widget build(BuildContext context) {
    double screenWidth = MediaQuery.of(context).size.width;
    double screenHeight = MediaQuery.of(context).size.height;

    return Container();
  }
}